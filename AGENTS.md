# AIエージェントのためのプロジェクトルールとガイドライン

このファイルは、プロジェクトで作業するAIエージェント（Antigravityなど）のルールとガイドラインを定義します。

## 開発ルール

- クリーンでドキュメント化されたコードを書くこと。
- 新機能のテストを書くこと。
- コードのモジュール化とメンテナンス性を維持すること。
- **Gitによる細粒度な変更管理**:
  - 作業は一括で行わず、論理的な単位（関数追加、バグ修正、ドキュメント更新など）ごとに細かくコミットを分けること。
  - ユーザーからの明示的な指示を待つことなく、実装や修正の論理的な区切りごとに自律的かつ細やかに Git コミットを実行すること。
- **相対パスの原則**:
  - ドキュメントやソースコード内にファイル等のパスやリンクを記述する場合は、原則としてリポジトリルートからの**相対パス**を使用すること（絶対パスや `file://` スキームの仕様は避けること）。


## Git運用ルール

- **わかりやすく明確なコミットメッセージ**:
  - 何を変更したのかが一目でわかるコミットメッセージを作成すること（例: `feat:`, `fix:`, `docs:` などのプレフィックスの使用を推奨）。
  - コミットメッセージの説明文は日本語で記述すること。
- **機密情報のコミット禁止**:
  - `credentials.json` や環境変数、APIキーなどの個人情報や機密情報は絶対に Git 追跡対象（コミット）に含めないこと（`.gitignore` の設定に従うこと）。
- **不要なファイルの除外**:
  - 一時ファイル、ローカルテストデータ、ログなどはコミットに含めないこと。

## 共有スクリプトと機能 (Shared Scripts & Capabilities)

このプロジェクトには、動画制作をサポートするためにAIエージェントが使用できる共有スクリプトが用意されています。必要に応じてこれらのスクリプトを実行し、素材を生成してください。
